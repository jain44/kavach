    <div className="min-h-screen flex gradient-bg">
      {/* Left Panel */}
      <div className="hidden lg:flex flex-col justify-between w-[520px] p-12 border-r border-white/5">
        <div>
          <div className="flex items-center gap-3 mb-16">
            <div className="w-10 h-10 rounded-xl flex items-center justify-center" style={{ background: 'linear-gradient(135deg, var(--accent-gold), #aa8a34)' }}>
              <Shield size={20} className="text-black" />
            </div>
            <div>
              <div className="font-display font-bold text-white text-lg tracking-wider">SENTINEL</div>
              <div className="text-xs" style={{ color: 'var(--text-muted)' }}>IDBI Innovate 2026</div>
            </div>
          </div>

          <motion.div initial={{ opacity: 0, y: 20 }} animate={{ opacity: 1, y: 0 }} transition={{ delay: 0.1 }}>
            <h1 className="font-display font-bold text-4xl text-white leading-tight mb-6">
              Early Warning<br />
              <span style={{ color: 'var(--accent-gold)' }}>for MSME Credit</span>
            </h1>
            <p className="text-base leading-relaxed" style={{ color: 'var(--text-secondary)' }}>
              AI-driven default prediction 12 months ahead. Fusing structured financial data with GST signals, bureau trends, and transaction behaviour — all in a unified Risk Grade.
            </p>
          </motion.div>
        </div>

        {/* Stats */}
        <motion.div initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ delay: 0.3 }} className="space-y-4">
          {[
            { label: 'AUC-ROC', value: '0.7294', target: 'honest baseline', color: 'var(--accent-gold)' },
            { label: 'Precision @ Top 10%', value: '39.2%', target: 'honest baseline', color: 'var(--accent-gold)' },
            { label: 'Alert Lead Time', value: '12 months', target: 'vs. reactive baseline', color: 'var(--accent-gold)' },
            { label: 'Accuracy Lift', value: '~1.8×', target: 'over 20.8% default rate', color: 'var(--accent-gold)' },
          ].map((stat) => (
            <div key={stat.label} className="flex items-center justify-between p-3 rounded-xl" style={{ background: 'rgba(201,168,76,0.03)', border: '1px solid var(--border-subtle)' }}>
              <span className="text-sm" style={{ color: 'var(--text-secondary)' }}>{stat.label}</span>
              <div className="text-right">
                <div className="font-display font-bold text-base" style={{ color: stat.color }}>{stat.value}</div>
                <div className="text-xs" style={{ color: 'var(--text-muted)' }}>{stat.target}</div>
              </div>
            </div>
          ))}
        </motion.div>
      </div>

      {/* Right Panel — Login Form */}
      <div className="flex-1 flex items-center justify-center p-8">
        <motion.div
          initial={{ opacity: 0, scale: 0.97 }}
          animate={{ opacity: 1, scale: 1 }}
          transition={{ duration: 0.35 }}
          className="w-full max-w-md"
        >
          {/* Mobile logo */}
          <div className="flex items-center gap-3 mb-8 lg:hidden">
            <div className="w-9 h-9 rounded-xl flex items-center justify-center" style={{ background: 'linear-gradient(135deg, var(--accent-gold), #aa8a34)' }}>
              <Shield size={18} className="text-black" />
            </div>
            <div className="font-display font-bold text-white text-lg tracking-wider">SENTINEL</div>
          </div>

          <div className="glass-card p-8">
            <h2 className="font-display font-bold text-2xl text-white mb-1">Sign In</h2>
            <p className="text-sm mb-8" style={{ color: 'var(--text-muted)' }}>Select your role to access Sentinel</p>

            <form onSubmit={handleLogin} className="space-y-5">
              {/* Role Selector */}
              <div>
                <label className="text-xs font-semibold mb-2 block" style={{ color: 'var(--text-muted)', textTransform: 'uppercase', letterSpacing: '0.05em' }}>
                  Role
                </label>
                <div className="relative">
                  <button
                    type="button"
                    onClick={() => setShowRoleDropdown(!showRoleDropdown)}
                    className="w-full input-field text-left flex items-center justify-between"
                  >
                    <div className="flex items-center gap-2">
                      <div className="w-2 h-2 rounded-full" style={{ background: selectedRole.color }} />
                      <span style={{ color: 'var(--text-primary)' }}>{selectedRole.label}</span>
                    </div>
                    <ChevronDown size={14} style={{ color: 'var(--text-muted)' }} className={`transition-transform ${showRoleDropdown ? 'rotate-180' : ''}`} />
                  </button>

                  {showRoleDropdown && (
                    <div className="absolute top-full left-0 right-0 mt-1 rounded-xl overflow-hidden z-50 shadow-2xl" style={{ background: 'var(--bg-surface)', border: '1px solid var(--border-muted)' }}>
                      {DEMO_CREDENTIALS.map((cred) => (
                        <button
                          key={cred.role}
                          type="button"
                          onClick={() => { setSelectedRole(cred); setShowRoleDropdown(false) }}
                          className="w-full flex items-start gap-3 px-4 py-3 text-left transition-colors hover:bg-white/5"
                          style={{ background: selectedRole.role === cred.role ? 'rgba(201,168,76,0.06)' : 'transparent' }}
                        >
                          <div className="w-2 h-2 rounded-full mt-1.5 flex-shrink-0" style={{ background: cred.color }} />
                          <div>
                            <div className="text-sm font-medium text-white">{cred.label}</div>
                            <div className="text-xs" style={{ color: 'var(--text-muted)' }}>{cred.description}</div>
                          </div>
                        </button>
                      ))}
                    </div>
                  )}
                </div>
              </div>

              {/* Username */}
              <div>
                <label className="text-xs font-semibold mb-2 block" style={{ color: 'var(--text-muted)', textTransform: 'uppercase', letterSpacing: '0.05em' }}>
                  Username
                </label>
                <input
                  className="input-field"
                  value={selectedRole.username}
                  readOnly
                  style={{ color: 'var(--text-muted)' }}
                />
              </div>

              {/* Password */}
              <div>
                <label className="text-xs font-semibold mb-2 block" style={{ color: 'var(--text-muted)', textTransform: 'uppercase', letterSpacing: '0.05em' }}>
                  Password
                </label>
                <div className="relative">
                  <input
                    className="input-field pr-10"
                    type={showPass ? 'text' : 'password'}
                    value={password}
                    onChange={(e) => setPassword(e.target.value)}
                  />
                  <button
                    type="button"
                    onClick={() => setShowPass(!showPass)}
                    className="absolute right-3 top-1/2 -translate-y-1/2"
                    style={{ color: 'var(--text-muted)' }}
                  >
                    {showPass ? <EyeOff size={15} /> : <Eye size={15} />}
                  </button>
                </div>
              </div>

              {error && (
                <div className="text-sm p-3 rounded-lg" style={{ background: 'rgba(239,68,68,0.1)', color: '#ef4444', border: '1px solid rgba(239,68,68,0.2)' }}>
                  {error}
                </div>
              )}

              <button
                type="submit"
                disabled={isLoading}
                className="btn-primary w-full flex items-center justify-center gap-2"
                style={{ opacity: isLoading ? 0.7 : 1 }}
              >
                {isLoading ? (
                  <>
                    <div className="w-4 h-4 border-2 border-black/30 border-t-black rounded-full animate-spin" />
                    Signing in...
                  </>
                ) : (
                  `Sign In as ${selectedRole.label}`
                )}
              </button>
            </form>

            {/* Demo hint */}
            <div className="mt-6 p-3 rounded-lg" style={{ background: 'rgba(201,168,76,0.04)', border: '1px solid var(--border-subtle)' }}>
              <div className="text-xs font-semibold mb-1" style={{ color: 'var(--accent-gold)' }}>Demo Credentials</div>
              <div className="text-xs" style={{ color: 'var(--text-muted)' }}>
                All roles use password: <span className="font-mono" style={{ color: 'var(--text-secondary)' }}>sentinel123</span>
              </div>
            </div>
          </div>

          <p className="text-center text-xs mt-6" style={{ color: 'var(--text-muted)' }}>
            IDBI Innovate 2026 — Track 04: MSME Credit Risk
          </p>
        </motion.div>
      </div>
    </div>